interface:
  display_name: "Portal Mini-App UI Fixer"
  short_description: "Fix UI in a specific MrSmith mini-app"
  default_prompt: "Use $portal-miniapp-ui-fixer to fix the UI in a specific app under apps/ while following the existing MrSmith planning and blocking review rules."
policy:
  allow_implicit_invocation: false
