apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mrsmith
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: mrsmith.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: mrsmith
                port:
                  number: 80
